# Config Package

Shared lint, prettier, and TypeScript configuration lives here so every app inherits consistent tooling.

## Purpose
- Centralize lint and formatting rules.
- Keep TypeScript configs aligned across services.

## How it will evolve
- Add ESLint and Prettier configs.
- Publish shared TS configs for server/client.
